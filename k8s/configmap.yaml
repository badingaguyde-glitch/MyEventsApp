apiVersion: v1
kind: ConfigMap
metadata:
  name: bantu-config
  namespace: bantu-production
data:
  # Configuration for the application
  PORT: "5000"
  REDIS_URL: "redis://redis-service:6379"
  SMTP_HOST: "smtp.example.com"
  SMTP_PORT: "587"
  SMTP_USER: "wetransferclone@gmail.com"
  FROM_NAME: "MyEvents App"
  FROM_EMAIL: "wetransferclone@gmail.com"
  FRONTEND_URL: "https://app.myevents.badinga.dev"
  EVENT_CREATION_FEE: "500"
  PAYMENT_TIMEOUT_HOURS: "2"